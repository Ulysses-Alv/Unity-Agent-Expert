# Collider interactions

When two **colliders** make contact, you can call functions to trigger other events in your project via scripting.

The two essential function types for **collision** interaction are `OnCollision` and `OnTrigger`.

| **Topic** | **Description** |
| --- | --- |
| [Use collisions to trigger other events](collider-interactions-other-events.md) | Overview of collision events and triggers, and the scripting API you need to use. |
| [Interaction between collider types](collider-types-interaction.md) | How the different collider types interact with each other to call `OnCollision` and `OnTrigger` events. |
| [OnCollision events](collider-interactions-oncollision.md) | How colliders can call events when they physically collide. |
| [OnTrigger events](collider-interactions-ontrigger.md) | How colliders can call events when one enters the space of another in a non-physical collision. |
| [Create and configure a trigger collider](collider-interactions-create-trigger.md) | Create a trigger collider and configure its associated **GameObject** to correctly call events on trigger interactions. |
| [Example scripts for collider events](collider-interactions-example-scripts.md) | Example **scripts** for `OnCollision` and `OnTrigger` events. These example scripts demonstrate some potential uses for scripted collision interactions and events. |
# Textures in the Render Graph system in URP

How to access and use textures in custom render passes in the Universal **Render Pipeline** (URP).

| Page | Description |
| --- | --- |
| [Read or write to a texture in a render pass](render-graph-read-write-texture.md) | Set a texture as an input or output for a render pass. |
| [Create a temporary texture for a single frame](render-graph-create-a-texture.md) | Create a temporary texture in a render pass, also known as an internal resource. |
| [Use a texture in multiple render passes](render-graph-pass-textures-between-passes.md) | Point render graph to an external texture, set a texture as a global texture, or add a texture to the frame data. |

## Additional resources

* [Use frame data](accessing-frame-data.md)
* The **blit** examples in the [URP RenderGraph Samples](package-sample-urp-package-samples.md)